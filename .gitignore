# prisma generated client (v7 output in-repo, regenerasi via `prisma generate`)
kaos-kami-web/src/generated/
# dependencies (semua workspace, bukan cuma root)
node_modules/
.pnp
.pnp.js

# testing
/coverage

# next.js
.next/
out/

# mobile & native build artifacts
**/android/.gradle/
**/android/app/build/
**/android/build/
**/ios/App/build/
**/ios/App/Pods/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# cloudflare / wrangler
.wrangler

# agents / ide scratch
.agents
.skills-sourced

# OpenNext
.open-next

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example

# secrets
.env
.env.local
.dev.vars
# android signing — JANGAN PERNAH commit keystore/password (insiden Sep 2026)
*.keystore
*.jks
key.properties
android/key.properties
**/android/key.properties

# local sqlite + keystore password stashes (never commit)
kaos-kami-web/prisma/dev.db
keystore-passwords.local.txt
**/keystore-passwords.local.txt

# build/preview/deploy logs (never commit)
rebuild*.log
preview*.log
deploy*.log
*-build*.log
testworker.log
