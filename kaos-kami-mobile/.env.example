# Salin ke .env.local (JANGAN commit) — dibaca aplikasi Capacitor saat build.
NEXT_PUBLIC_API_URL=https://kaoskami.biz.id
# Kontak/array toko (opsional — ada fallback resmi di kode).
# NEXT_PUBLIC_SHOP_CONTACT_WHATSAPP=6281244002026
# NEXT_PUBLIC_SHOP_WORKSHOP_ADDRESS=
# Uji internal dev SAJA: tampilkan tombol "Simulasikan Pelanggan Sudah Lunas"
# di AdminMobileDashboard. JANGAN set =1 di build rilis (default mati).
# NEXT_PUBLIC_ALLOW_PAYMENT_SIMULATION=1
